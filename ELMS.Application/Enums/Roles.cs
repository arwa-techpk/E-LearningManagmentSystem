namespace ELMCOM.Application.Enums
{
    public enum Roles
    {
        SuperAdmin, // This role will Register the school
        Admin, // This is the School Admin, also register his School
        Teacher, // This is Teacher inside the School
        Student // This is Student inside the School
    }
}